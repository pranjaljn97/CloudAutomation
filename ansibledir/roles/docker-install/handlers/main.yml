---
# sudo systemctl enable docker
- name: Start docker on boot
  systemd:
    name: docker
    state: started
    enabled: yes

- name: reload systemd
  become: true
  command: systemctl daemon-reload    